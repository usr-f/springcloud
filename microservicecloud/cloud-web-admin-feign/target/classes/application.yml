server:
  port: 8905
spring:
  application:
    name: web-admin-feign
  thymeleaf:
    cache: false
    mode: LEGACYHTML5
    encoding: UTF-8
    servlet:
      content-type: text/html
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7001/eureka/
feign:
  hystrix:
    enabled: true
